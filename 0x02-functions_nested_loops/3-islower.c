#include "main.h"
#include <stdio.h>

/**
 * main - print lower alphabets
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
_islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
return (0)
}
