#include "main.h"

/**
 * _isdigit - checks for a didgit
 *
 * @c: character to be checked
 *
 * Return: 1 if condition is true, 0 otherwise
 */

int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
return (0)
}
