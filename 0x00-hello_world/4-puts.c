#include <stdio.h>
/**
 * main - Entry point
 *
 * return always 0 (success)
 * Return: the main function will return an integer
 */
int main(void)
{
puts("Programming is like building a multilingual puzzle");
return (0);
}
